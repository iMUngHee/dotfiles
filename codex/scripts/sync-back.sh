#!/bin/bash
# codex/scripts/sync-back.sh — placeholder.
# Codex side currently has no editable artifacts that need to flow back into
# the repo (config.toml is hand-managed, AGENTS.md is generated). This stub
# exists for symmetry with claude/scripts/sync-back.sh and to give future
# codex-specific syncs a home.
set -euo pipefail
exit 0
