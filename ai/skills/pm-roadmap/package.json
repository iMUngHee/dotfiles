{
  "name": "pm-roadmap",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "for t in *.test.ts; do tsx \"$t\" || exit 1; done"
  },
  "dependencies": {
    "tsx": "^4.0.0",
    "@types/node": "^22.0.0"
  }
}
