{
  "name": "pm-context",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "./node_modules/.bin/tsx server.ts",
    "test": "for t in *.test.ts; do ./node_modules/.bin/tsx \"$t\" || exit 1; done"
  },
  "dependencies": {
    "tsx": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
